const INT_STATE = {
    id: 'user_id_test',
    role: 'customer'
};

const userReducer = (state=INT_STATE, action) => {
    switch(action.type){

        default: 
            return state
    }
};

export default userReducer;