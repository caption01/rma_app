const INTIAL_STATE = {
    id: 'user_id_test',
    role: 'customer'
};

const userReducer = (state=INTIAL_STATE, action) => {
    switch(action.type){

        default: 
            return state
    }
};

export default userReducer;